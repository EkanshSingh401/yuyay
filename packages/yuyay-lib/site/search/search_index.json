{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"YUYAY Intelligence Framework","text":"<p>YUYAY \u2014 from the Quechua word for knowledge \u2014 is a multi-dimensional intelligence framework for evaluating alignment across 12 archetype dimensions.</p>"},{"location":"#install","title":"Install","text":"<pre><code>pip install yuyay\n</code></pre>"},{"location":"#quick-example","title":"Quick Example","text":"<pre><code>from yuyay.questionnaire import process_responses\n\nreport = process_responses({\"1a\": \"YES\", \"1b\": \"NO\", \"2a\": \"PO\"})\nprint(report.yes_count, report.no_count, report.po_count)\nprint(report.flags)\n</code></pre>"},{"location":"#cli","title":"CLI","text":"<pre><code>yuyay archetypes\nyuyay transformers\nyuyay evaluate --responses \"1a=YES,1b=NO,2a=PO\"\nyuyay query --prompt \"What is wisdom?\" --provider anthropic\n</code></pre>"},{"location":"#links","title":"Links","text":"<ul> <li>Live Platform</li> <li>API Docs</li> <li>PyPI</li> <li>GitHub</li> </ul>"},{"location":"contributing/","title":"Contributing","text":"<p>See the full CONTRIBUTING.md on GitHub.</p>"},{"location":"quickstart/","title":"Quickstart","text":""},{"location":"quickstart/#installation","title":"Installation","text":"<pre><code>pip install yuyay\n</code></pre>"},{"location":"quickstart/#running-an-evaluation","title":"Running an Evaluation","text":"<pre><code>from yuyay.questionnaire import process_responses\n\nresponses = {\n    \"1a\": \"YES\", \"1b\": \"NO\",\n    \"2a\": \"PO\", \"2b\": \"YES\",\n    \"3\": \"NO\", \"4\": \"YES\",\n    \"5\": \"NO\", \"6\": \"PO\",\n    \"7\": \"YES\", \"8\": \"NO\",\n    \"9\": \"YES\", \"10\": \"NO\",\n}\n\nreport = process_responses(responses)\nprint(f\"YES: {report.yes_count}\")\nprint(f\"NO: {report.no_count}\")\nprint(f\"PO: {report.po_count}\")\nprint(f\"Flagged: {report.flags}\")\nprint(report.summary())\n</code></pre>"},{"location":"quickstart/#querying-fios","title":"Querying FIOS","text":"<pre><code>import asyncio\nfrom yuyay.fios import FIOS, FIOSConfig\n\nasync def main():\n    config = FIOSConfig(provider=\"anthropic\", model=\"claude-sonnet-4-6\")\n    fios = FIOS(config)\n    result = await fios.query(\"What does it mean to act with wisdom?\")\n    print(result.response)\n    print(f\"Coherence score: {result.coherence_score}/100\")\n    print(f\"Cost: ${result.estimated_cost_usd:.6f}\")\n\nasyncio.run(main())\n</code></pre>"},{"location":"quickstart/#using-the-cli","title":"Using the CLI","text":"<pre><code># List archetypes\nyuyay archetypes\n\n# List transformer questions  \nyuyay transformers\n\n# Run evaluation\nyuyay evaluate --responses \"1a=YES,1b=NO,2a=PO,2b=YES,3=NO,4=YES,5=NO,6=PO,7=YES,8=NO,9=YES,10=NO\"\n\n# Query FIOS\nyuyay query --prompt \"What is wisdom?\" --provider anthropic\n</code></pre>"},{"location":"api/archetypes/","title":"Archetypes","text":""},{"location":"api/archetypes/#yuyay.archetypes","title":"<code>yuyay.archetypes</code>","text":"<p>YUYAY Archetypes Module</p> <p>Defines the 12 archetypes of the YUYAY Intelligence framework. Each archetype represents a distinct mode of intelligence and pattern of action within any conscious system.</p>"},{"location":"api/archetypes/#yuyay.archetypes.Archetype","title":"<code>Archetype</code>  <code>dataclass</code>","text":"Source code in <code>yuyay/archetypes.py</code> <pre><code>@dataclass\nclass Archetype:\n    name: str\n    function: str\n    gifts: str\n    shadow: str\n\n    def summary(self) -&gt; str:\n        \"\"\"Return a one-line summary of the archetype.\"\"\"\n        return f\"{self.name}: {self.function}\"\n\n    def is_shadow_active(self, traits: list[str]) -&gt; bool:\n        \"\"\"Check if any shadow traits are present in a given trait list.\n\n        Args:\n            traits: A list of trait strings to check against the shadow.\n\n        Returns:\n            True if any shadow keyword appears in the traits list.\n        \"\"\"\n        shadow_keywords = [word.lower() for word in self.shadow.split(\", \")]\n        return any(trait.lower() in shadow_keywords for trait in traits)\n</code></pre>"},{"location":"api/archetypes/#yuyay.archetypes.Archetype.is_shadow_active","title":"<code>is_shadow_active(traits)</code>","text":"<p>Check if any shadow traits are present in a given trait list.</p> <p>Parameters:</p> Name Type Description Default <code>traits</code> <code>list[str]</code> <p>A list of trait strings to check against the shadow.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if any shadow keyword appears in the traits list.</p> Source code in <code>yuyay/archetypes.py</code> <pre><code>def is_shadow_active(self, traits: list[str]) -&gt; bool:\n    \"\"\"Check if any shadow traits are present in a given trait list.\n\n    Args:\n        traits: A list of trait strings to check against the shadow.\n\n    Returns:\n        True if any shadow keyword appears in the traits list.\n    \"\"\"\n    shadow_keywords = [word.lower() for word in self.shadow.split(\", \")]\n    return any(trait.lower() in shadow_keywords for trait in traits)\n</code></pre>"},{"location":"api/archetypes/#yuyay.archetypes.Archetype.summary","title":"<code>summary()</code>","text":"<p>Return a one-line summary of the archetype.</p> Source code in <code>yuyay/archetypes.py</code> <pre><code>def summary(self) -&gt; str:\n    \"\"\"Return a one-line summary of the archetype.\"\"\"\n    return f\"{self.name}: {self.function}\"\n</code></pre>"},{"location":"api/archetypes/#yuyay.archetypes.get_archetype_by_name","title":"<code>get_archetype_by_name(name)</code>","text":"<p>Look up an archetype by name (case-insensitive).</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The archetype name to search for.</p> required <p>Returns:</p> Type Description <code>Archetype | None</code> <p>The matching Archetype, or None if not found.</p> Source code in <code>yuyay/archetypes.py</code> <pre><code>def get_archetype_by_name(name: str) -&gt; Archetype | None:\n    \"\"\"Look up an archetype by name (case-insensitive).\n\n    Args:\n        name: The archetype name to search for.\n\n    Returns:\n        The matching Archetype, or None if not found.\n    \"\"\"\n    name_lower = name.lower()\n    for archetype in ALL_ARCHETYPES:\n        if name_lower in archetype.name.lower():\n            return archetype\n    return None\n</code></pre>"},{"location":"api/cli/","title":"CLI","text":""},{"location":"api/cli/#yuyay.cli","title":"<code>yuyay.cli</code>","text":"<p>YUYAY Intelligence Framework \u2014 Command Line Interface.</p>"},{"location":"api/cli/#yuyay.cli.archetypes","title":"<code>archetypes()</code>","text":"<p>List all 12 YUYAY archetypes.</p> Source code in <code>yuyay/cli.py</code> <pre><code>@app.command()\ndef archetypes() -&gt; None:\n    \"\"\"List all 12 YUYAY archetypes.\"\"\"\n    table = Table(title=\"YUYAY Archetypes\", show_header=True)\n    table.add_column(\"Name\", style=\"gold1\")\n    table.add_column(\"Function\", style=\"white\")\n\n    for archetype in ALL_ARCHETYPES:\n        table.add_row(archetype.name, archetype.function)\n\n    console.print(table)\n</code></pre>"},{"location":"api/cli/#yuyay.cli.evaluate","title":"<code>evaluate(responses=typer.Option(..., '--responses', '-r', help=\"Comma-separated responses e.g. '1a=YES,1b=NO,2a=PO'\"))</code>","text":"<p>Run a YUYAY evaluation with YES/NO/PO responses.</p> Source code in <code>yuyay/cli.py</code> <pre><code>@app.command()\ndef evaluate(\n    responses: str = typer.Option(\n        ...,\n        \"--responses\",\n        \"-r\",\n        help=\"Comma-separated responses e.g. '1a=YES,1b=NO,2a=PO'\",\n    ),\n) -&gt; None:\n    \"\"\"Run a YUYAY evaluation with YES/NO/PO responses.\"\"\"\n    try:\n        parsed: dict[str, str] = {}\n        for item in responses.split(\",\"):\n            key, value = item.strip().split(\"=\")\n            parsed[key.strip()] = value.strip().upper()\n\n        report = process_responses(parsed)\n\n        console.print(\n            Panel(\n                f\"[green]YES:[/green] {report.yes_count}  \"\n                f\"[red]NO:[/red] {report.no_count}  \"\n                f\"[yellow]PO:[/yellow] {report.po_count}  \"\n                f\"[white]Total:[/white] {report.total}  \"\n                f\"[magenta]Flagged:[/magenta] {len(report.flags)}\",\n                title=\"[gold1]YUYAY Evaluation Report[/gold1]\",\n            )\n        )\n\n        if report.flags:\n            console.print(\"\\n[yellow]Dimensions for deeper inquiry:[/yellow]\")\n            for dim in report.flags:\n                console.print(f\"  \u2022 {dim}\")\n\n    except Exception as e:\n        console.print(f\"[red]Error:[/red] {str(e)}\")\n        raise typer.Exit(1)\n</code></pre>"},{"location":"api/cli/#yuyay.cli.main","title":"<code>main()</code>","text":"<p>Entry point for the YUYAY CLI.</p> Source code in <code>yuyay/cli.py</code> <pre><code>def main() -&gt; None:\n    \"\"\"Entry point for the YUYAY CLI.\"\"\"\n    app()\n</code></pre>"},{"location":"api/cli/#yuyay.cli.query","title":"<code>query(prompt=typer.Option(..., '--prompt', '-p', help='Your question'), provider=typer.Option('anthropic', '--provider', help='LLM provider'), model=typer.Option('claude-sonnet-4-6', '--model', help='Model name'), api_key=typer.Option(None, '--api-key', help='API key'))</code>","text":"<p>Query the FIOS intelligence layer.</p> Source code in <code>yuyay/cli.py</code> <pre><code>@app.command()\ndef query(\n    prompt: str = typer.Option(..., \"--prompt\", \"-p\", help=\"Your question\"),\n    provider: str = typer.Option(\"anthropic\", \"--provider\", help=\"LLM provider\"),\n    model: str = typer.Option(\"claude-sonnet-4-6\", \"--model\", help=\"Model name\"),\n    api_key: Optional[str] = typer.Option(None, \"--api-key\", help=\"API key\"),\n) -&gt; None:\n    \"\"\"Query the FIOS intelligence layer.\"\"\"\n\n    async def _query() -&gt; None:\n        config = FIOSConfig(\n            provider=provider,\n            model=model,\n            api_key=api_key or \"\",\n        )\n        fios = FIOS(config)\n        with console.status(f\"[gold1]Querying {provider}...[/gold1]\"):\n            result = await fios.query(prompt)\n\n        console.print(\n            Panel(\n                result.response,\n                title=f\"[gold1]{result.provider}/{result.model}[/gold1]\",\n            )\n        )\n        console.print(\n            f\"[dim]Coherence: {result.coherence_score}/100 | \"\n            f\"Tokens: {result.total_tokens} | \"\n            f\"Latency: {result.latency_ms:.0f}ms | \"\n            f\"Cost: ${result.estimated_cost_usd:.6f}[/dim]\"\n        )\n\n    asyncio.run(_query())\n</code></pre>"},{"location":"api/cli/#yuyay.cli.transformers","title":"<code>transformers()</code>","text":"<p>List all transformer questions.</p> Source code in <code>yuyay/cli.py</code> <pre><code>@app.command()\ndef transformers() -&gt; None:\n    \"\"\"List all transformer questions.\"\"\"\n    table = Table(title=\"Transformer Questions\", show_header=True)\n    table.add_column(\"ID\", style=\"gold1\")\n    table.add_column(\"Question\", style=\"white\")\n\n    for transformer in ALL_TRANSFORMERS:\n        table.add_row(transformer.id, transformer.question)\n\n    console.print(table)\n</code></pre>"},{"location":"api/fios/","title":"FIOS","text":""},{"location":"api/fios/#yuyay.fios","title":"<code>yuyay.fios</code>","text":"<p>FIOS \u2014 Foundational Intelligent OS for the YUYAY Intelligence Framework.</p> <p>FIOS is an LLM orchestration layer that injects YUYAY framework context into any LLM provider query and evaluates the response against YUYAY coherence metrics.</p>"},{"location":"api/fios/#yuyay.fios.AnthropicAdapter","title":"<code>AnthropicAdapter</code>","text":"<p>               Bases: <code>LLMProvider</code></p> <p>FIOS adapter for Anthropic Claude API.</p> Source code in <code>yuyay/fios.py</code> <pre><code>class AnthropicAdapter(LLMProvider):\n    \"\"\"FIOS adapter for Anthropic Claude API.\"\"\"\n\n    @retry(\n        stop=stop_after_attempt(3),\n        wait=wait_exponential(multiplier=1, min=1, max=10),\n        retry=retry_if_not_exception_type(RuntimeError),\n    )\n    async def query(self, prompt: str) -&gt; FIOSResult:\n        \"\"\"Send a prompt to Claude and return a FIOSResult.\n\n        Retries up to 3 times with exponential backoff on failure.\n\n        Args:\n            prompt: The full enriched prompt to send.\n\n        Returns:\n            A FIOSResult with Claude's response, token usage, and cost.\n\n        Raises:\n            RuntimeError: If the circuit breaker is open.\n        \"\"\"\n        if self.circuit_breaker.is_open():\n            raise RuntimeError(\n                f\"Circuit breaker open for {self.config.provider} \u2014 \"\n                f\"too many failures, try again later.\"\n            )\n        try:\n            import anthropic\n\n            client = anthropic.AsyncAnthropic(\n                api_key=self.config.api_key or os.environ.get(\"ANTHROPIC_API_KEY\")\n            )\n            context = build_yuyay_context()\n            full_prompt = f\"{context}\\n\\nUser Query: {prompt}\"\n\n            start = time.monotonic()\n            message = await client.messages.create(\n                model=self.config.model,\n                max_tokens=self.config.max_tokens,\n                messages=[{\"role\": \"user\", \"content\": full_prompt}],\n            )\n            latency_ms = (time.monotonic() - start) * 1000\n\n            response_text = message.content[0].text\n            coherence_score, flags = evaluate_coherence(response_text)\n            cost = estimate_cost(\n                \"anthropic\",\n                message.usage.input_tokens,\n                message.usage.output_tokens,\n            )\n            self.circuit_breaker.record_success()\n            return FIOSResult(\n                provider=\"anthropic\",\n                model=self.config.model,\n                prompt=full_prompt,\n                response=response_text,\n                input_tokens=message.usage.input_tokens,\n                output_tokens=message.usage.output_tokens,\n                latency_ms=latency_ms,\n                coherence_score=coherence_score,\n                flags=flags,\n                estimated_cost_usd=cost,\n            )\n        except RuntimeError:\n            raise\n        except Exception as e:\n            self.circuit_breaker.record_failure()\n            raise e\n</code></pre>"},{"location":"api/fios/#yuyay.fios.AnthropicAdapter.query","title":"<code>query(prompt)</code>  <code>async</code>","text":"<p>Send a prompt to Claude and return a FIOSResult.</p> <p>Retries up to 3 times with exponential backoff on failure.</p> <p>Parameters:</p> Name Type Description Default <code>prompt</code> <code>str</code> <p>The full enriched prompt to send.</p> required <p>Returns:</p> Type Description <code>FIOSResult</code> <p>A FIOSResult with Claude's response, token usage, and cost.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the circuit breaker is open.</p> Source code in <code>yuyay/fios.py</code> <pre><code>@retry(\n    stop=stop_after_attempt(3),\n    wait=wait_exponential(multiplier=1, min=1, max=10),\n    retry=retry_if_not_exception_type(RuntimeError),\n)\nasync def query(self, prompt: str) -&gt; FIOSResult:\n    \"\"\"Send a prompt to Claude and return a FIOSResult.\n\n    Retries up to 3 times with exponential backoff on failure.\n\n    Args:\n        prompt: The full enriched prompt to send.\n\n    Returns:\n        A FIOSResult with Claude's response, token usage, and cost.\n\n    Raises:\n        RuntimeError: If the circuit breaker is open.\n    \"\"\"\n    if self.circuit_breaker.is_open():\n        raise RuntimeError(\n            f\"Circuit breaker open for {self.config.provider} \u2014 \"\n            f\"too many failures, try again later.\"\n        )\n    try:\n        import anthropic\n\n        client = anthropic.AsyncAnthropic(\n            api_key=self.config.api_key or os.environ.get(\"ANTHROPIC_API_KEY\")\n        )\n        context = build_yuyay_context()\n        full_prompt = f\"{context}\\n\\nUser Query: {prompt}\"\n\n        start = time.monotonic()\n        message = await client.messages.create(\n            model=self.config.model,\n            max_tokens=self.config.max_tokens,\n            messages=[{\"role\": \"user\", \"content\": full_prompt}],\n        )\n        latency_ms = (time.monotonic() - start) * 1000\n\n        response_text = message.content[0].text\n        coherence_score, flags = evaluate_coherence(response_text)\n        cost = estimate_cost(\n            \"anthropic\",\n            message.usage.input_tokens,\n            message.usage.output_tokens,\n        )\n        self.circuit_breaker.record_success()\n        return FIOSResult(\n            provider=\"anthropic\",\n            model=self.config.model,\n            prompt=full_prompt,\n            response=response_text,\n            input_tokens=message.usage.input_tokens,\n            output_tokens=message.usage.output_tokens,\n            latency_ms=latency_ms,\n            coherence_score=coherence_score,\n            flags=flags,\n            estimated_cost_usd=cost,\n        )\n    except RuntimeError:\n        raise\n    except Exception as e:\n        self.circuit_breaker.record_failure()\n        raise e\n</code></pre>"},{"location":"api/fios/#yuyay.fios.CircuitBreaker","title":"<code>CircuitBreaker</code>","text":"<p>Circuit breaker for LLM provider calls.</p> <p>Tracks failures across requests and opens the circuit when too many failures occur, preventing cascading failures and wasted resources.</p> <p>Attributes:</p> Name Type Description <code>failure_threshold</code> <p>Number of failures before opening the circuit.</p> <code>recovery_timeout</code> <p>Seconds to wait before trying again after opening.</p> <code>provider</code> <p>The provider name this circuit breaker protects.</p> Source code in <code>yuyay/fios.py</code> <pre><code>class CircuitBreaker:\n    \"\"\"Circuit breaker for LLM provider calls.\n\n    Tracks failures across requests and opens the circuit when too many\n    failures occur, preventing cascading failures and wasted resources.\n\n    Attributes:\n        failure_threshold: Number of failures before opening the circuit.\n        recovery_timeout: Seconds to wait before trying again after opening.\n        provider: The provider name this circuit breaker protects.\n    \"\"\"\n\n    def __init__(\n        self,\n        provider: str,\n        failure_threshold: int = 5,\n        recovery_timeout: float = 60.0,\n    ) -&gt; None:\n        \"\"\"Initialize the circuit breaker.\n\n        Args:\n            provider: The provider name this circuit breaker protects.\n            failure_threshold: Failures before opening. Defaults to 5.\n            recovery_timeout: Seconds before half-open retry. Defaults to 60.\n        \"\"\"\n        self.provider = provider\n        self.failure_threshold = failure_threshold\n        self.recovery_timeout = recovery_timeout\n        self._state = CircuitState.CLOSED\n        self._failure_count = 0\n        self._last_failure_time: float = 0.0\n\n    @property\n    def state(self) -&gt; CircuitState:\n        \"\"\"Return the current circuit state, updating if recovery timeout passed.\n\n        Returns:\n            The current CircuitState.\n        \"\"\"\n        if (\n            self._state == CircuitState.OPEN\n            and time.monotonic() - self._last_failure_time &gt;= self.recovery_timeout\n        ):\n            self._state = CircuitState.HALF_OPEN\n        return self._state\n\n    def record_success(self) -&gt; None:\n        \"\"\"Record a successful call \u2014 reset failure count and close circuit.\"\"\"\n        self._failure_count = 0\n        self._state = CircuitState.CLOSED\n\n    def record_failure(self) -&gt; None:\n        \"\"\"Record a failed call \u2014 increment count and open circuit if threshold hit.\"\"\"\n        self._failure_count += 1\n        self._last_failure_time = time.monotonic()\n        if self._failure_count &gt;= self.failure_threshold:\n            self._state = CircuitState.OPEN\n\n    def is_open(self) -&gt; bool:\n        \"\"\"Check if the circuit is open and requests should be blocked.\n\n        Returns:\n            True if the circuit is open, False otherwise.\n        \"\"\"\n        return self.state == CircuitState.OPEN\n</code></pre>"},{"location":"api/fios/#yuyay.fios.CircuitBreaker.state","title":"<code>state</code>  <code>property</code>","text":"<p>Return the current circuit state, updating if recovery timeout passed.</p> <p>Returns:</p> Type Description <code>CircuitState</code> <p>The current CircuitState.</p>"},{"location":"api/fios/#yuyay.fios.CircuitBreaker.__init__","title":"<code>__init__(provider, failure_threshold=5, recovery_timeout=60.0)</code>","text":"<p>Initialize the circuit breaker.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>The provider name this circuit breaker protects.</p> required <code>failure_threshold</code> <code>int</code> <p>Failures before opening. Defaults to 5.</p> <code>5</code> <code>recovery_timeout</code> <code>float</code> <p>Seconds before half-open retry. Defaults to 60.</p> <code>60.0</code> Source code in <code>yuyay/fios.py</code> <pre><code>def __init__(\n    self,\n    provider: str,\n    failure_threshold: int = 5,\n    recovery_timeout: float = 60.0,\n) -&gt; None:\n    \"\"\"Initialize the circuit breaker.\n\n    Args:\n        provider: The provider name this circuit breaker protects.\n        failure_threshold: Failures before opening. Defaults to 5.\n        recovery_timeout: Seconds before half-open retry. Defaults to 60.\n    \"\"\"\n    self.provider = provider\n    self.failure_threshold = failure_threshold\n    self.recovery_timeout = recovery_timeout\n    self._state = CircuitState.CLOSED\n    self._failure_count = 0\n    self._last_failure_time: float = 0.0\n</code></pre>"},{"location":"api/fios/#yuyay.fios.CircuitBreaker.is_open","title":"<code>is_open()</code>","text":"<p>Check if the circuit is open and requests should be blocked.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if the circuit is open, False otherwise.</p> Source code in <code>yuyay/fios.py</code> <pre><code>def is_open(self) -&gt; bool:\n    \"\"\"Check if the circuit is open and requests should be blocked.\n\n    Returns:\n        True if the circuit is open, False otherwise.\n    \"\"\"\n    return self.state == CircuitState.OPEN\n</code></pre>"},{"location":"api/fios/#yuyay.fios.CircuitBreaker.record_failure","title":"<code>record_failure()</code>","text":"<p>Record a failed call \u2014 increment count and open circuit if threshold hit.</p> Source code in <code>yuyay/fios.py</code> <pre><code>def record_failure(self) -&gt; None:\n    \"\"\"Record a failed call \u2014 increment count and open circuit if threshold hit.\"\"\"\n    self._failure_count += 1\n    self._last_failure_time = time.monotonic()\n    if self._failure_count &gt;= self.failure_threshold:\n        self._state = CircuitState.OPEN\n</code></pre>"},{"location":"api/fios/#yuyay.fios.CircuitBreaker.record_success","title":"<code>record_success()</code>","text":"<p>Record a successful call \u2014 reset failure count and close circuit.</p> Source code in <code>yuyay/fios.py</code> <pre><code>def record_success(self) -&gt; None:\n    \"\"\"Record a successful call \u2014 reset failure count and close circuit.\"\"\"\n    self._failure_count = 0\n    self._state = CircuitState.CLOSED\n</code></pre>"},{"location":"api/fios/#yuyay.fios.CircuitState","title":"<code>CircuitState</code>","text":"<p>               Bases: <code>Enum</code></p> <p>States for the circuit breaker pattern.</p> <p>Attributes:</p> Name Type Description <code>CLOSED</code> <p>Circuit is closed \u2014 requests flow through normally.</p> <code>OPEN</code> <p>Circuit is open \u2014 requests are blocked immediately.</p> <code>HALF_OPEN</code> <p>Circuit is testing \u2014 one request allowed through.</p> Source code in <code>yuyay/fios.py</code> <pre><code>class CircuitState(Enum):\n    \"\"\"States for the circuit breaker pattern.\n\n    Attributes:\n        CLOSED: Circuit is closed \u2014 requests flow through normally.\n        OPEN: Circuit is open \u2014 requests are blocked immediately.\n        HALF_OPEN: Circuit is testing \u2014 one request allowed through.\n    \"\"\"\n\n    CLOSED = \"closed\"\n    OPEN = \"open\"\n    HALF_OPEN = \"half_open\"\n</code></pre>"},{"location":"api/fios/#yuyay.fios.FIOS","title":"<code>FIOS</code>","text":"<p>Foundational Intelligent OS \u2014 main orchestrator for LLM queries.</p> <p>FIOS manages provider selection, prompt enrichment with YUYAY context, response evaluation, and result aggregation.</p> <p>Attributes:</p> Name Type Description <code>config</code> <p>The FIOSConfig defining provider and model settings.</p> <code>provider</code> <p>The concrete LLMProvider adapter instance.</p> Source code in <code>yuyay/fios.py</code> <pre><code>class FIOS:\n    \"\"\"Foundational Intelligent OS \u2014 main orchestrator for LLM queries.\n\n    FIOS manages provider selection, prompt enrichment with YUYAY context,\n    response evaluation, and result aggregation.\n\n    Attributes:\n        config: The FIOSConfig defining provider and model settings.\n        provider: The concrete LLMProvider adapter instance.\n    \"\"\"\n\n    PROVIDER_MAP: dict[str, type[LLMProvider]] = {\n        \"anthropic\": AnthropicAdapter,\n        \"openai\": OpenAIAdapter,\n        \"google\": GoogleAdapter,\n        \"mock\": MockAdapter,\n    }\n\n    def __init__(self, config: FIOSConfig) -&gt; None:\n        \"\"\"Initialize FIOS with a provider config.\n\n        Args:\n            config: The FIOSConfig specifying which provider and model to use.\n\n        Raises:\n            ValueError: If the provider name is not supported.\n        \"\"\"\n        if config.provider not in self.PROVIDER_MAP:\n            raise ValueError(\n                f\"Unsupported provider '{config.provider}'. \"\n                f\"Must be one of: {list(self.PROVIDER_MAP.keys())}\"\n            )\n        self.config = config\n        self.provider = self.PROVIDER_MAP[config.provider](config)\n\n    async def query(self, prompt: str) -&gt; FIOSResult:\n        \"\"\"Send a prompt through FIOS and return an evaluated result.\n\n        Enriches the prompt with YUYAY context, sends it to the configured\n        provider, and evaluates the response for coherence.\n\n        Args:\n            prompt: The user query to send.\n\n        Returns:\n            A FIOSResult with the response and evaluation metadata.\n        \"\"\"\n        return await self.provider.query(prompt)\n\n    async def query_all_providers(\n        self,\n        prompt: str,\n        configs: list[FIOSConfig],\n    ) -&gt; list[FIOSResult]:\n        \"\"\"Query multiple LLM providers concurrently and return all results.\n\n        Sends the same prompt to all configured providers simultaneously\n        using asyncio.gather for concurrent execution. Results include\n        coherence scores for comparison across providers.\n\n        Args:\n            prompt: The user query to send to all providers.\n            configs: List of FIOSConfig objects for each provider to query.\n\n        Returns:\n            A list of FIOSResult objects, one per provider, in the same\n            order as the configs list. Failed providers return None results\n            are excluded from the output.\n        \"\"\"\n\n        async def query_single(config: FIOSConfig) -&gt; FIOSResult | None:\n            \"\"\"Query a single provider and handle failures gracefully.\n\n            Args:\n                config: The FIOSConfig for this provider.\n\n            Returns:\n                A FIOSResult or None if the query failed.\n            \"\"\"\n            try:\n                fios = FIOS(config)\n                return await fios.query(prompt)\n            except Exception:\n                return None\n\n        results = await asyncio.gather(\n            *[query_single(config) for config in configs],\n        )\n        return [r for r in results if r is not None]\n</code></pre>"},{"location":"api/fios/#yuyay.fios.FIOS.__init__","title":"<code>__init__(config)</code>","text":"<p>Initialize FIOS with a provider config.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>FIOSConfig</code> <p>The FIOSConfig specifying which provider and model to use.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If the provider name is not supported.</p> Source code in <code>yuyay/fios.py</code> <pre><code>def __init__(self, config: FIOSConfig) -&gt; None:\n    \"\"\"Initialize FIOS with a provider config.\n\n    Args:\n        config: The FIOSConfig specifying which provider and model to use.\n\n    Raises:\n        ValueError: If the provider name is not supported.\n    \"\"\"\n    if config.provider not in self.PROVIDER_MAP:\n        raise ValueError(\n            f\"Unsupported provider '{config.provider}'. \"\n            f\"Must be one of: {list(self.PROVIDER_MAP.keys())}\"\n        )\n    self.config = config\n    self.provider = self.PROVIDER_MAP[config.provider](config)\n</code></pre>"},{"location":"api/fios/#yuyay.fios.FIOS.query","title":"<code>query(prompt)</code>  <code>async</code>","text":"<p>Send a prompt through FIOS and return an evaluated result.</p> <p>Enriches the prompt with YUYAY context, sends it to the configured provider, and evaluates the response for coherence.</p> <p>Parameters:</p> Name Type Description Default <code>prompt</code> <code>str</code> <p>The user query to send.</p> required <p>Returns:</p> Type Description <code>FIOSResult</code> <p>A FIOSResult with the response and evaluation metadata.</p> Source code in <code>yuyay/fios.py</code> <pre><code>async def query(self, prompt: str) -&gt; FIOSResult:\n    \"\"\"Send a prompt through FIOS and return an evaluated result.\n\n    Enriches the prompt with YUYAY context, sends it to the configured\n    provider, and evaluates the response for coherence.\n\n    Args:\n        prompt: The user query to send.\n\n    Returns:\n        A FIOSResult with the response and evaluation metadata.\n    \"\"\"\n    return await self.provider.query(prompt)\n</code></pre>"},{"location":"api/fios/#yuyay.fios.FIOS.query_all_providers","title":"<code>query_all_providers(prompt, configs)</code>  <code>async</code>","text":"<p>Query multiple LLM providers concurrently and return all results.</p> <p>Sends the same prompt to all configured providers simultaneously using asyncio.gather for concurrent execution. Results include coherence scores for comparison across providers.</p> <p>Parameters:</p> Name Type Description Default <code>prompt</code> <code>str</code> <p>The user query to send to all providers.</p> required <code>configs</code> <code>list[FIOSConfig]</code> <p>List of FIOSConfig objects for each provider to query.</p> required <p>Returns:</p> Type Description <code>list[FIOSResult]</code> <p>A list of FIOSResult objects, one per provider, in the same</p> <code>list[FIOSResult]</code> <p>order as the configs list. Failed providers return None results</p> <code>list[FIOSResult]</code> <p>are excluded from the output.</p> Source code in <code>yuyay/fios.py</code> <pre><code>async def query_all_providers(\n    self,\n    prompt: str,\n    configs: list[FIOSConfig],\n) -&gt; list[FIOSResult]:\n    \"\"\"Query multiple LLM providers concurrently and return all results.\n\n    Sends the same prompt to all configured providers simultaneously\n    using asyncio.gather for concurrent execution. Results include\n    coherence scores for comparison across providers.\n\n    Args:\n        prompt: The user query to send to all providers.\n        configs: List of FIOSConfig objects for each provider to query.\n\n    Returns:\n        A list of FIOSResult objects, one per provider, in the same\n        order as the configs list. Failed providers return None results\n        are excluded from the output.\n    \"\"\"\n\n    async def query_single(config: FIOSConfig) -&gt; FIOSResult | None:\n        \"\"\"Query a single provider and handle failures gracefully.\n\n        Args:\n            config: The FIOSConfig for this provider.\n\n        Returns:\n            A FIOSResult or None if the query failed.\n        \"\"\"\n        try:\n            fios = FIOS(config)\n            return await fios.query(prompt)\n        except Exception:\n            return None\n\n    results = await asyncio.gather(\n        *[query_single(config) for config in configs],\n    )\n    return [r for r in results if r is not None]\n</code></pre>"},{"location":"api/fios/#yuyay.fios.FIOSConfig","title":"<code>FIOSConfig</code>  <code>dataclass</code>","text":"<p>Configuration for a FIOS provider.</p> <p>Attributes:</p> Name Type Description <code>provider</code> <code>str</code> <p>The LLM provider name \u2014 anthropic, openai, or google.</p> <code>model</code> <code>str</code> <p>The specific model to use.</p> <code>max_tokens</code> <code>int</code> <p>Maximum tokens in the response.</p> <code>temperature</code> <code>float</code> <p>Sampling temperature from 0.0 to 1.0.</p> <code>api_key</code> <code>str</code> <p>The API key for the provider.</p> Source code in <code>yuyay/fios.py</code> <pre><code>@dataclass\nclass FIOSConfig:\n    \"\"\"Configuration for a FIOS provider.\n\n    Attributes:\n        provider: The LLM provider name \u2014 anthropic, openai, or google.\n        model: The specific model to use.\n        max_tokens: Maximum tokens in the response.\n        temperature: Sampling temperature from 0.0 to 1.0.\n        api_key: The API key for the provider.\n    \"\"\"\n\n    provider: str\n    model: str\n    max_tokens: int = 1000\n    temperature: float = 0.7\n    api_key: str = \"\"\n</code></pre>"},{"location":"api/fios/#yuyay.fios.FIOSResult","title":"<code>FIOSResult</code>  <code>dataclass</code>","text":"<p>Result of a FIOS query.</p> <p>Attributes:</p> Name Type Description <code>provider</code> <code>str</code> <p>The LLM provider used.</p> <code>model</code> <code>str</code> <p>The specific model used.</p> <code>prompt</code> <code>str</code> <p>The full enriched prompt sent to the LLM.</p> <code>response</code> <code>str</code> <p>The raw response from the LLM.</p> <code>input_tokens</code> <code>int</code> <p>Number of input tokens used.</p> <code>output_tokens</code> <code>int</code> <p>Number of output tokens used.</p> <code>latency_ms</code> <code>float</code> <p>Time taken for the API call in milliseconds.</p> <code>coherence_score</code> <code>int</code> <p>YUYAY coherence score from 0 to 100.</p> <code>flags</code> <code>list[str]</code> <p>List of coherence issues found in the response.</p> Source code in <code>yuyay/fios.py</code> <pre><code>@dataclass\nclass FIOSResult:\n    \"\"\"Result of a FIOS query.\n\n    Attributes:\n        provider: The LLM provider used.\n        model: The specific model used.\n        prompt: The full enriched prompt sent to the LLM.\n        response: The raw response from the LLM.\n        input_tokens: Number of input tokens used.\n        output_tokens: Number of output tokens used.\n        latency_ms: Time taken for the API call in milliseconds.\n        coherence_score: YUYAY coherence score from 0 to 100.\n        flags: List of coherence issues found in the response.\n    \"\"\"\n\n    provider: str\n    model: str\n    prompt: str\n    response: str\n    input_tokens: int\n    output_tokens: int\n    latency_ms: float\n    coherence_score: int\n    flags: list[str] = field(default_factory=list)\n    estimated_cost_usd: float = 0.0\n\n    @property\n    def total_tokens(self) -&gt; int:\n        \"\"\"Total tokens used in this query.\n\n        Returns:\n            Sum of input and output tokens.\n        \"\"\"\n        return self.input_tokens + self.output_tokens\n\n    def summary(self) -&gt; str:\n        \"\"\"Return a formatted summary of this FIOS result.\n\n        Returns:\n            A string showing provider, tokens, latency, and coherence score.\n        \"\"\"\n        return (\n            f\"[{self.provider}/{self.model}] \"\n            f\"Tokens: {self.total_tokens} | \"\n            f\"Latency: {self.latency_ms:.0f}ms | \"\n            f\"Coherence: {self.coherence_score}/100 | \"\n            f\"Cost: ${self.estimated_cost_usd:.6f}\"\n        )\n</code></pre>"},{"location":"api/fios/#yuyay.fios.FIOSResult.total_tokens","title":"<code>total_tokens</code>  <code>property</code>","text":"<p>Total tokens used in this query.</p> <p>Returns:</p> Type Description <code>int</code> <p>Sum of input and output tokens.</p>"},{"location":"api/fios/#yuyay.fios.FIOSResult.summary","title":"<code>summary()</code>","text":"<p>Return a formatted summary of this FIOS result.</p> <p>Returns:</p> Type Description <code>str</code> <p>A string showing provider, tokens, latency, and coherence score.</p> Source code in <code>yuyay/fios.py</code> <pre><code>def summary(self) -&gt; str:\n    \"\"\"Return a formatted summary of this FIOS result.\n\n    Returns:\n        A string showing provider, tokens, latency, and coherence score.\n    \"\"\"\n    return (\n        f\"[{self.provider}/{self.model}] \"\n        f\"Tokens: {self.total_tokens} | \"\n        f\"Latency: {self.latency_ms:.0f}ms | \"\n        f\"Coherence: {self.coherence_score}/100 | \"\n        f\"Cost: ${self.estimated_cost_usd:.6f}\"\n    )\n</code></pre>"},{"location":"api/fios/#yuyay.fios.GoogleAdapter","title":"<code>GoogleAdapter</code>","text":"<p>               Bases: <code>LLMProvider</code></p> <p>FIOS adapter for Google Gemini API.</p> Source code in <code>yuyay/fios.py</code> <pre><code>class GoogleAdapter(LLMProvider):\n    \"\"\"FIOS adapter for Google Gemini API.\"\"\"\n\n    @retry(\n        stop=stop_after_attempt(3),\n        wait=wait_exponential(multiplier=1, min=1, max=10),\n        retry=retry_if_not_exception_type(RuntimeError),\n    )\n    async def query(self, prompt: str) -&gt; FIOSResult:\n        \"\"\"Send a prompt to Gemini and return a FIOSResult.\n\n        Retries up to 3 times with exponential backoff on failure.\n\n        Args:\n            prompt: The full enriched prompt to send.\n\n        Returns:\n            A FIOSResult with Gemini's response and cost.\n\n        Raises:\n            RuntimeError: If the circuit breaker is open.\n        \"\"\"\n        if self.circuit_breaker.is_open():\n            raise RuntimeError(\n                f\"Circuit breaker open for {self.config.provider} \u2014 \"\n                f\"too many failures, try again later.\"\n            )\n        try:\n            from google import genai\n\n            client = genai.Client(\n                api_key=self.config.api_key or os.environ.get(\"GOOGLE_API_KEY\")\n            )\n            context = build_yuyay_context()\n            full_prompt = f\"{context}\\n\\nUser Query: {prompt}\"\n\n            start = time.monotonic()\n            response = await client.aio.models.generate_content(\n                model=self.config.model,\n                contents=full_prompt,\n            )\n            latency_ms = (time.monotonic() - start) * 1000\n\n            response_text = response.text or \"\"\n            coherence_score, flags = evaluate_coherence(response_text)\n            usage = response.usage_metadata\n            input_tokens = usage.prompt_token_count or 0 if usage else 0\n            output_tokens = usage.candidates_token_count or 0 if usage else 0\n            cost = estimate_cost(\"google\", input_tokens, output_tokens)\n            self.circuit_breaker.record_success()\n            return FIOSResult(\n                provider=\"google\",\n                model=self.config.model,\n                prompt=full_prompt,\n                response=response_text,\n                input_tokens=input_tokens,\n                output_tokens=output_tokens,\n                latency_ms=latency_ms,\n                coherence_score=coherence_score,\n                flags=flags,\n                estimated_cost_usd=cost,\n            )\n        except RuntimeError:\n            raise\n        except Exception as e:\n            self.circuit_breaker.record_failure()\n            raise e\n</code></pre>"},{"location":"api/fios/#yuyay.fios.GoogleAdapter.query","title":"<code>query(prompt)</code>  <code>async</code>","text":"<p>Send a prompt to Gemini and return a FIOSResult.</p> <p>Retries up to 3 times with exponential backoff on failure.</p> <p>Parameters:</p> Name Type Description Default <code>prompt</code> <code>str</code> <p>The full enriched prompt to send.</p> required <p>Returns:</p> Type Description <code>FIOSResult</code> <p>A FIOSResult with Gemini's response and cost.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the circuit breaker is open.</p> Source code in <code>yuyay/fios.py</code> <pre><code>@retry(\n    stop=stop_after_attempt(3),\n    wait=wait_exponential(multiplier=1, min=1, max=10),\n    retry=retry_if_not_exception_type(RuntimeError),\n)\nasync def query(self, prompt: str) -&gt; FIOSResult:\n    \"\"\"Send a prompt to Gemini and return a FIOSResult.\n\n    Retries up to 3 times with exponential backoff on failure.\n\n    Args:\n        prompt: The full enriched prompt to send.\n\n    Returns:\n        A FIOSResult with Gemini's response and cost.\n\n    Raises:\n        RuntimeError: If the circuit breaker is open.\n    \"\"\"\n    if self.circuit_breaker.is_open():\n        raise RuntimeError(\n            f\"Circuit breaker open for {self.config.provider} \u2014 \"\n            f\"too many failures, try again later.\"\n        )\n    try:\n        from google import genai\n\n        client = genai.Client(\n            api_key=self.config.api_key or os.environ.get(\"GOOGLE_API_KEY\")\n        )\n        context = build_yuyay_context()\n        full_prompt = f\"{context}\\n\\nUser Query: {prompt}\"\n\n        start = time.monotonic()\n        response = await client.aio.models.generate_content(\n            model=self.config.model,\n            contents=full_prompt,\n        )\n        latency_ms = (time.monotonic() - start) * 1000\n\n        response_text = response.text or \"\"\n        coherence_score, flags = evaluate_coherence(response_text)\n        usage = response.usage_metadata\n        input_tokens = usage.prompt_token_count or 0 if usage else 0\n        output_tokens = usage.candidates_token_count or 0 if usage else 0\n        cost = estimate_cost(\"google\", input_tokens, output_tokens)\n        self.circuit_breaker.record_success()\n        return FIOSResult(\n            provider=\"google\",\n            model=self.config.model,\n            prompt=full_prompt,\n            response=response_text,\n            input_tokens=input_tokens,\n            output_tokens=output_tokens,\n            latency_ms=latency_ms,\n            coherence_score=coherence_score,\n            flags=flags,\n            estimated_cost_usd=cost,\n        )\n    except RuntimeError:\n        raise\n    except Exception as e:\n        self.circuit_breaker.record_failure()\n        raise e\n</code></pre>"},{"location":"api/fios/#yuyay.fios.LLMProvider","title":"<code>LLMProvider</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for all FIOS LLM provider adapters.</p> <p>All concrete provider implementations must inherit from this class and implement the query method.</p> Source code in <code>yuyay/fios.py</code> <pre><code>class LLMProvider(abc.ABC):\n    \"\"\"Abstract base class for all FIOS LLM provider adapters.\n\n    All concrete provider implementations must inherit from this class\n    and implement the query method.\n    \"\"\"\n\n    def __init__(self, config: FIOSConfig) -&gt; None:\n        \"\"\"Initialize the provider with a config.\n\n        Args:\n            config: The FIOSConfig for this provider.\n        \"\"\"\n        self.config = config\n        self.circuit_breaker = CircuitBreaker(provider=config.provider)\n\n    @abc.abstractmethod\n    async def query(self, prompt: str) -&gt; FIOSResult:\n        \"\"\"Send a prompt to the LLM and return a FIOSResult.\n\n        Args:\n            prompt: The full enriched prompt to send.\n\n        Returns:\n            A FIOSResult with the response and metadata.\n        \"\"\"\n        ...\n</code></pre>"},{"location":"api/fios/#yuyay.fios.LLMProvider.__init__","title":"<code>__init__(config)</code>","text":"<p>Initialize the provider with a config.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>FIOSConfig</code> <p>The FIOSConfig for this provider.</p> required Source code in <code>yuyay/fios.py</code> <pre><code>def __init__(self, config: FIOSConfig) -&gt; None:\n    \"\"\"Initialize the provider with a config.\n\n    Args:\n        config: The FIOSConfig for this provider.\n    \"\"\"\n    self.config = config\n    self.circuit_breaker = CircuitBreaker(provider=config.provider)\n</code></pre>"},{"location":"api/fios/#yuyay.fios.LLMProvider.query","title":"<code>query(prompt)</code>  <code>abstractmethod</code> <code>async</code>","text":"<p>Send a prompt to the LLM and return a FIOSResult.</p> <p>Parameters:</p> Name Type Description Default <code>prompt</code> <code>str</code> <p>The full enriched prompt to send.</p> required <p>Returns:</p> Type Description <code>FIOSResult</code> <p>A FIOSResult with the response and metadata.</p> Source code in <code>yuyay/fios.py</code> <pre><code>@abc.abstractmethod\nasync def query(self, prompt: str) -&gt; FIOSResult:\n    \"\"\"Send a prompt to the LLM and return a FIOSResult.\n\n    Args:\n        prompt: The full enriched prompt to send.\n\n    Returns:\n        A FIOSResult with the response and metadata.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"api/fios/#yuyay.fios.MockAdapter","title":"<code>MockAdapter</code>","text":"<p>               Bases: <code>LLMProvider</code></p> <p>Mock FIOS adapter for testing without real API calls.</p> Source code in <code>yuyay/fios.py</code> <pre><code>class MockAdapter(LLMProvider):\n    \"\"\"Mock FIOS adapter for testing without real API calls.\"\"\"\n\n    async def query(self, prompt: str) -&gt; FIOSResult:\n        \"\"\"Return a mock response for testing.\n\n        Args:\n            prompt: The prompt \u2014 ignored in mock mode.\n\n        Returns:\n            A FIOSResult with fake data for testing.\n\n        Raises:\n            RuntimeError: If the circuit breaker is open.\n        \"\"\"\n        if self.circuit_breaker.is_open():\n            raise RuntimeError(\n                f\"Circuit breaker open for {self.config.provider} \u2014 \"\n                f\"too many failures, try again later.\"\n            )\n        context = build_yuyay_context()\n        mock_response = (\n            \"This response demonstrates wisdom, compassion, and purpose. \"\n            \"It considers the balance and harmony of whole systems, \"\n            \"and aims to heal the planet for future generations.\"\n        )\n        coherence_score, flags = evaluate_coherence(mock_response)\n        self.circuit_breaker.record_success()\n        return FIOSResult(\n            provider=\"mock\",\n            model=\"mock-model\",\n            prompt=f\"{context}\\n\\nUser Query: {prompt}\",\n            response=mock_response,\n            input_tokens=100,\n            output_tokens=50,\n            latency_ms=10.0,\n            coherence_score=coherence_score,\n            flags=flags,\n            estimated_cost_usd=0.0,\n        )\n</code></pre>"},{"location":"api/fios/#yuyay.fios.MockAdapter.query","title":"<code>query(prompt)</code>  <code>async</code>","text":"<p>Return a mock response for testing.</p> <p>Parameters:</p> Name Type Description Default <code>prompt</code> <code>str</code> <p>The prompt \u2014 ignored in mock mode.</p> required <p>Returns:</p> Type Description <code>FIOSResult</code> <p>A FIOSResult with fake data for testing.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the circuit breaker is open.</p> Source code in <code>yuyay/fios.py</code> <pre><code>async def query(self, prompt: str) -&gt; FIOSResult:\n    \"\"\"Return a mock response for testing.\n\n    Args:\n        prompt: The prompt \u2014 ignored in mock mode.\n\n    Returns:\n        A FIOSResult with fake data for testing.\n\n    Raises:\n        RuntimeError: If the circuit breaker is open.\n    \"\"\"\n    if self.circuit_breaker.is_open():\n        raise RuntimeError(\n            f\"Circuit breaker open for {self.config.provider} \u2014 \"\n            f\"too many failures, try again later.\"\n        )\n    context = build_yuyay_context()\n    mock_response = (\n        \"This response demonstrates wisdom, compassion, and purpose. \"\n        \"It considers the balance and harmony of whole systems, \"\n        \"and aims to heal the planet for future generations.\"\n    )\n    coherence_score, flags = evaluate_coherence(mock_response)\n    self.circuit_breaker.record_success()\n    return FIOSResult(\n        provider=\"mock\",\n        model=\"mock-model\",\n        prompt=f\"{context}\\n\\nUser Query: {prompt}\",\n        response=mock_response,\n        input_tokens=100,\n        output_tokens=50,\n        latency_ms=10.0,\n        coherence_score=coherence_score,\n        flags=flags,\n        estimated_cost_usd=0.0,\n    )\n</code></pre>"},{"location":"api/fios/#yuyay.fios.OpenAIAdapter","title":"<code>OpenAIAdapter</code>","text":"<p>               Bases: <code>LLMProvider</code></p> <p>FIOS adapter for OpenAI GPT API.</p> Source code in <code>yuyay/fios.py</code> <pre><code>class OpenAIAdapter(LLMProvider):\n    \"\"\"FIOS adapter for OpenAI GPT API.\"\"\"\n\n    @retry(\n        stop=stop_after_attempt(3),\n        wait=wait_exponential(multiplier=1, min=1, max=10),\n        retry=retry_if_not_exception_type(RuntimeError),\n    )\n    async def query(self, prompt: str) -&gt; FIOSResult:\n        \"\"\"Send a prompt to GPT and return a FIOSResult.\n\n        Retries up to 3 times with exponential backoff on failure.\n\n        Args:\n            prompt: The full enriched prompt to send.\n\n        Returns:\n            A FIOSResult with GPT's response, token usage, and cost.\n\n        Raises:\n            RuntimeError: If the circuit breaker is open.\n        \"\"\"\n        if self.circuit_breaker.is_open():\n            raise RuntimeError(\n                f\"Circuit breaker open for {self.config.provider} \u2014 \"\n                f\"too many failures, try again later.\"\n            )\n        try:\n            import openai\n\n            client = openai.AsyncOpenAI(\n                api_key=self.config.api_key or os.environ.get(\"OPENAI_API_KEY\")\n            )\n            context = build_yuyay_context()\n\n            start = time.monotonic()\n            completion = await client.chat.completions.create(\n                model=self.config.model,\n                max_completion_tokens=self.config.max_tokens,\n                messages=[\n                    {\"role\": \"system\", \"content\": context},\n                    {\"role\": \"user\", \"content\": prompt},\n                ],\n            )\n            latency_ms = (time.monotonic() - start) * 1000\n\n            response_text = completion.choices[0].message.content or \"\"\n            input_tokens = completion.usage.prompt_tokens if completion.usage else 0\n            output_tokens = (\n                completion.usage.completion_tokens if completion.usage else 0\n            )\n            coherence_score, flags = evaluate_coherence(response_text)\n            cost = estimate_cost(\"openai\", input_tokens, output_tokens)\n            self.circuit_breaker.record_success()\n            return FIOSResult(\n                provider=\"openai\",\n                model=self.config.model,\n                prompt=prompt,\n                response=response_text,\n                input_tokens=input_tokens,\n                output_tokens=output_tokens,\n                latency_ms=latency_ms,\n                coherence_score=coherence_score,\n                flags=flags,\n                estimated_cost_usd=cost,\n            )\n        except RuntimeError:\n            raise\n        except Exception as e:\n            self.circuit_breaker.record_failure()\n            raise e\n</code></pre>"},{"location":"api/fios/#yuyay.fios.OpenAIAdapter.query","title":"<code>query(prompt)</code>  <code>async</code>","text":"<p>Send a prompt to GPT and return a FIOSResult.</p> <p>Retries up to 3 times with exponential backoff on failure.</p> <p>Parameters:</p> Name Type Description Default <code>prompt</code> <code>str</code> <p>The full enriched prompt to send.</p> required <p>Returns:</p> Type Description <code>FIOSResult</code> <p>A FIOSResult with GPT's response, token usage, and cost.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the circuit breaker is open.</p> Source code in <code>yuyay/fios.py</code> <pre><code>@retry(\n    stop=stop_after_attempt(3),\n    wait=wait_exponential(multiplier=1, min=1, max=10),\n    retry=retry_if_not_exception_type(RuntimeError),\n)\nasync def query(self, prompt: str) -&gt; FIOSResult:\n    \"\"\"Send a prompt to GPT and return a FIOSResult.\n\n    Retries up to 3 times with exponential backoff on failure.\n\n    Args:\n        prompt: The full enriched prompt to send.\n\n    Returns:\n        A FIOSResult with GPT's response, token usage, and cost.\n\n    Raises:\n        RuntimeError: If the circuit breaker is open.\n    \"\"\"\n    if self.circuit_breaker.is_open():\n        raise RuntimeError(\n            f\"Circuit breaker open for {self.config.provider} \u2014 \"\n            f\"too many failures, try again later.\"\n        )\n    try:\n        import openai\n\n        client = openai.AsyncOpenAI(\n            api_key=self.config.api_key or os.environ.get(\"OPENAI_API_KEY\")\n        )\n        context = build_yuyay_context()\n\n        start = time.monotonic()\n        completion = await client.chat.completions.create(\n            model=self.config.model,\n            max_completion_tokens=self.config.max_tokens,\n            messages=[\n                {\"role\": \"system\", \"content\": context},\n                {\"role\": \"user\", \"content\": prompt},\n            ],\n        )\n        latency_ms = (time.monotonic() - start) * 1000\n\n        response_text = completion.choices[0].message.content or \"\"\n        input_tokens = completion.usage.prompt_tokens if completion.usage else 0\n        output_tokens = (\n            completion.usage.completion_tokens if completion.usage else 0\n        )\n        coherence_score, flags = evaluate_coherence(response_text)\n        cost = estimate_cost(\"openai\", input_tokens, output_tokens)\n        self.circuit_breaker.record_success()\n        return FIOSResult(\n            provider=\"openai\",\n            model=self.config.model,\n            prompt=prompt,\n            response=response_text,\n            input_tokens=input_tokens,\n            output_tokens=output_tokens,\n            latency_ms=latency_ms,\n            coherence_score=coherence_score,\n            flags=flags,\n            estimated_cost_usd=cost,\n        )\n    except RuntimeError:\n        raise\n    except Exception as e:\n        self.circuit_breaker.record_failure()\n        raise e\n</code></pre>"},{"location":"api/fios/#yuyay.fios.build_yuyay_context","title":"<code>build_yuyay_context()</code>","text":"<p>Build the YUYAY framework context string for prompt injection.</p> <p>Combines all archetypes and transformer questions into a system context that gets prepended to every LLM query.</p> <p>Returns:</p> Type Description <code>str</code> <p>A formatted string containing the full YUYAY framework context.</p> Source code in <code>yuyay/fios.py</code> <pre><code>def build_yuyay_context() -&gt; str:\n    \"\"\"Build the YUYAY framework context string for prompt injection.\n\n    Combines all archetypes and transformer questions into a system context\n    that gets prepended to every LLM query.\n\n    Returns:\n        A formatted string containing the full YUYAY framework context.\n    \"\"\"\n    archetype_context = \"\\n\".join(f\"- {a.name}: {a.function}\" for a in ALL_ARCHETYPES)\n    transformer_context = \"\\n\".join(\n        f\"- [{t.id}] {t.question}\" for t in ALL_TRANSFORMERS\n    )\n    return f\"\"\"You are operating within the YUYAY Intelligence Framework.\n\nYUYAY ARCHETYPES (12 dimensions of human potential):\n{archetype_context}\n\nTRANSFORMER QUESTIONS (apply these to your response):\n{transformer_context}\n\nWhen responding, consider all 12 archetype dimensions and apply the transformer\nquestions to ensure your response serves the highest purpose, is wise, compassionate,\nand contributes to healing the planet. Use PO lateral thinking when facing uncertainty.\"\"\"\n</code></pre>"},{"location":"api/fios/#yuyay.fios.estimate_cost","title":"<code>estimate_cost(provider, input_tokens, output_tokens)</code>","text":"<p>Estimate the cost of a query in USD.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>str</code> <p>The LLM provider name.</p> required <code>input_tokens</code> <code>int</code> <p>Number of input tokens used.</p> required <code>output_tokens</code> <code>int</code> <p>Number of output tokens used.</p> required <p>Returns:</p> Type Description <code>float</code> <p>Estimated cost in USD rounded to 6 decimal places.</p> Source code in <code>yuyay/fios.py</code> <pre><code>def estimate_cost(provider: str, input_tokens: int, output_tokens: int) -&gt; float:\n    \"\"\"Estimate the cost of a query in USD.\n\n    Args:\n        provider: The LLM provider name.\n        input_tokens: Number of input tokens used.\n        output_tokens: Number of output tokens used.\n\n    Returns:\n        Estimated cost in USD rounded to 6 decimal places.\n    \"\"\"\n    rates = COST_PER_MILLION_TOKENS.get(provider, {\"input\": 0.0, \"output\": 0.0})\n    input_cost = (input_tokens / 1_000_000) * rates[\"input\"]\n    output_cost = (output_tokens / 1_000_000) * rates[\"output\"]\n    return round(input_cost + output_cost, 6)\n</code></pre>"},{"location":"api/fios/#yuyay.fios.evaluate_coherence","title":"<code>evaluate_coherence(response)</code>","text":"<p>Evaluate a response against YUYAY coherence metrics.</p> <p>Checks whether the response demonstrates awareness of key YUYAY concepts like wisdom, compassion, purpose, and planetary healing.</p> <p>Parameters:</p> Name Type Description Default <code>response</code> <code>str</code> <p>The raw LLM response text to evaluate.</p> required <p>Returns:</p> Type Description <code>tuple[int, list[str]]</code> <p>A tuple of (score from 0 to 100, list of flags for missing concepts).</p> Source code in <code>yuyay/fios.py</code> <pre><code>def evaluate_coherence(response: str) -&gt; tuple[int, list[str]]:\n    \"\"\"Evaluate a response against YUYAY coherence metrics.\n\n    Checks whether the response demonstrates awareness of key YUYAY concepts\n    like wisdom, compassion, purpose, and planetary healing.\n\n    Args:\n        response: The raw LLM response text to evaluate.\n\n    Returns:\n        A tuple of (score from 0 to 100, list of flags for missing concepts).\n    \"\"\"\n    response_lower = response.lower()\n    score = 100\n    flags: list[str] = []\n\n    checks = [\n        (\"purpose\", [\"purpose\", \"goal\", \"mission\", \"serve\"]),\n        (\"wisdom\", [\"wisdom\", \"wise\", \"reflect\", \"consider\"]),\n        (\"compassion\", [\"compassion\", \"empathy\", \"care\", \"mercy\"]),\n        (\"planetary\", [\"planet\", \"earth\", \"heal\", \"future\", \"generation\"]),\n        (\"balance\", [\"balance\", \"harmony\", \"whole\", \"system\"]),\n    ]\n\n    for concept, keywords in checks:\n        if not any(kw in response_lower for kw in keywords):\n            score -= 20\n            flags.append(f\"missing_{concept}\")\n\n    return max(0, score), flags\n</code></pre>"},{"location":"api/po/","title":"PO","text":""},{"location":"api/po/#yuyay.po","title":"<code>yuyay.po</code>","text":"<p>Edward de Bono PO lateral thinking logic for the YUYAY Intelligence Framework.</p>"},{"location":"api/po/#yuyay.po.POResult","title":"<code>POResult</code>  <code>dataclass</code>","text":"<p>Result of a PO lateral thinking operation.</p> <p>Attributes:</p> Name Type Description <code>original</code> <code>str</code> <p>The original statement or idea.</p> <code>reframed</code> <code>str</code> <p>The reframed version using PO logic.</p> <code>challenge</code> <code>str</code> <p>The assumption being challenged.</p> Source code in <code>yuyay/po.py</code> <pre><code>@dataclass\nclass POResult:\n    \"\"\"Result of a PO lateral thinking operation.\n\n    Attributes:\n        original: The original statement or idea.\n        reframed: The reframed version using PO logic.\n        challenge: The assumption being challenged.\n    \"\"\"\n\n    original: str\n    reframed: str\n    challenge: str\n\n    def summary(self) -&gt; str:\n        \"\"\"Return a formatted summary of this PO result.\n\n        Returns:\n            A string showing the original, challenge, and reframe.\n        \"\"\"\n        return (\n            f\"Original: {self.original}\\n\"\n            f\"Challenge: {self.challenge}\\n\"\n            f\"Reframed: {self.reframed}\"\n        )\n</code></pre>"},{"location":"api/po/#yuyay.po.POResult.summary","title":"<code>summary()</code>","text":"<p>Return a formatted summary of this PO result.</p> <p>Returns:</p> Type Description <code>str</code> <p>A string showing the original, challenge, and reframe.</p> Source code in <code>yuyay/po.py</code> <pre><code>def summary(self) -&gt; str:\n    \"\"\"Return a formatted summary of this PO result.\n\n    Returns:\n        A string showing the original, challenge, and reframe.\n    \"\"\"\n    return (\n        f\"Original: {self.original}\\n\"\n        f\"Challenge: {self.challenge}\\n\"\n        f\"Reframed: {self.reframed}\"\n    )\n</code></pre>"},{"location":"api/po/#yuyay.po.po_challenge","title":"<code>po_challenge(assumption)</code>","text":"<p>Generate a PO challenge prompt for a given assumption.</p> <p>Parameters:</p> Name Type Description Default <code>assumption</code> <code>str</code> <p>The assumption to challenge.</p> required <p>Returns:</p> Type Description <code>str</code> <p>A string prompting lateral thinking about the assumption.</p> Source code in <code>yuyay/po.py</code> <pre><code>def po_challenge(assumption: str) -&gt; str:\n    \"\"\"Generate a PO challenge prompt for a given assumption.\n\n    Args:\n        assumption: The assumption to challenge.\n\n    Returns:\n        A string prompting lateral thinking about the assumption.\n    \"\"\"\n    return f\"PO: What if the opposite of '{assumption}' were true?\"\n</code></pre>"},{"location":"api/po/#yuyay.po.po_reframe","title":"<code>po_reframe(statement, reframe, challenge)</code>","text":"<p>Apply PO lateral thinking to reframe a statement.</p> <p>Parameters:</p> Name Type Description Default <code>statement</code> <code>str</code> <p>The original idea or statement to reframe.</p> required <code>reframe</code> <code>str</code> <p>The new perspective or alternative framing.</p> required <code>challenge</code> <code>str</code> <p>The assumption being challenged by PO.</p> required <p>Returns:</p> Type Description <code>POResult</code> <p>A POResult containing the original, reframe, and challenge.</p> Source code in <code>yuyay/po.py</code> <pre><code>def po_reframe(statement: str, reframe: str, challenge: str) -&gt; POResult:\n    \"\"\"Apply PO lateral thinking to reframe a statement.\n\n    Args:\n        statement: The original idea or statement to reframe.\n        reframe: The new perspective or alternative framing.\n        challenge: The assumption being challenged by PO.\n\n    Returns:\n        A POResult containing the original, reframe, and challenge.\n    \"\"\"\n    return POResult(\n        original=statement,\n        reframed=reframe,\n        challenge=challenge,\n    )\n</code></pre>"},{"location":"api/questionnaire/","title":"Questionnaire","text":""},{"location":"api/questionnaire/#yuyay.questionnaire","title":"<code>yuyay.questionnaire</code>","text":"<p>Questionnaire processor for the YUYAY Intelligence Framework.</p>"},{"location":"api/questionnaire/#yuyay.questionnaire.QuestionnaireReport","title":"<code>QuestionnaireReport</code>  <code>dataclass</code>","text":"<p>Result of processing a set of questionnaire responses.</p> <p>Attributes:</p> Name Type Description <code>yes_count</code> <code>int</code> <p>Number of YES responses.</p> <code>no_count</code> <code>int</code> <p>Number of NO responses.</p> <code>po_count</code> <code>int</code> <p>Number of PO responses.</p> <code>total</code> <code>int</code> <p>Total number of responses processed.</p> <code>flags</code> <code>list[str]</code> <p>List of question ids that returned NO or PO.</p> Source code in <code>yuyay/questionnaire.py</code> <pre><code>@dataclass\nclass QuestionnaireReport:\n    \"\"\"Result of processing a set of questionnaire responses.\n\n    Attributes:\n        yes_count: Number of YES responses.\n        no_count: Number of NO responses.\n        po_count: Number of PO responses.\n        total: Total number of responses processed.\n        flags: List of question ids that returned NO or PO.\n    \"\"\"\n\n    yes_count: int\n    no_count: int\n    po_count: int\n    total: int\n    flags: list[str] = field(default_factory=list)\n\n    def summary(self) -&gt; str:\n        \"\"\"Return a formatted summary of the questionnaire report.\n\n        Returns:\n            A string showing YES/NO/PO counts and flagged questions.\n        \"\"\"\n        return (\n            f\"YES: {self.yes_count} | NO: {self.no_count} | PO: {self.po_count} \"\n            f\"| Total: {self.total} | Flagged: {len(self.flags)}\"\n        )\n</code></pre>"},{"location":"api/questionnaire/#yuyay.questionnaire.QuestionnaireReport.summary","title":"<code>summary()</code>","text":"<p>Return a formatted summary of the questionnaire report.</p> <p>Returns:</p> Type Description <code>str</code> <p>A string showing YES/NO/PO counts and flagged questions.</p> Source code in <code>yuyay/questionnaire.py</code> <pre><code>def summary(self) -&gt; str:\n    \"\"\"Return a formatted summary of the questionnaire report.\n\n    Returns:\n        A string showing YES/NO/PO counts and flagged questions.\n    \"\"\"\n    return (\n        f\"YES: {self.yes_count} | NO: {self.no_count} | PO: {self.po_count} \"\n        f\"| Total: {self.total} | Flagged: {len(self.flags)}\"\n    )\n</code></pre>"},{"location":"api/questionnaire/#yuyay.questionnaire.process_responses","title":"<code>process_responses(responses)</code>","text":"<p>Process a set of transformer question responses.</p> <p>Parameters:</p> Name Type Description Default <code>responses</code> <code>dict[str, str]</code> <p>A dict mapping question id to response string (YES, NO, or PO).</p> required <p>Returns:</p> Type Description <code>QuestionnaireReport</code> <p>A QuestionnaireReport with counts and flagged question ids.</p> <p>Raises:</p> Type Description <code>InvalidResponseError</code> <p>If any response value is not YES, NO, or PO.</p> Source code in <code>yuyay/questionnaire.py</code> <pre><code>def process_responses(responses: dict[str, str]) -&gt; QuestionnaireReport:\n    \"\"\"Process a set of transformer question responses.\n\n    Args:\n        responses: A dict mapping question id to response string (YES, NO, or PO).\n\n    Returns:\n        A QuestionnaireReport with counts and flagged question ids.\n\n    Raises:\n        InvalidResponseError: If any response value is not YES, NO, or PO.\n    \"\"\"\n    yes_count = 0\n    no_count = 0\n    po_count = 0\n    flags: list[str] = []\n\n    for question_id, response in responses.items():\n        normalized = response.strip().upper()\n        if normalized not in VALID_RESPONSES:\n            raise InvalidResponseError(\n                f\"Invalid response '{response}' for question '{question_id}'. \"\n                f\"Must be YES, NO, or PO.\"\n            )\n        if normalized == \"YES\":\n            yes_count += 1\n        elif normalized == \"NO\":\n            no_count += 1\n            flags.append(question_id)\n        else:\n            po_count += 1\n            flags.append(question_id)\n\n    return QuestionnaireReport(\n        yes_count=yes_count,\n        no_count=no_count,\n        po_count=po_count,\n        total=len(responses),\n        flags=flags,\n    )\n</code></pre>"},{"location":"api/transformers/","title":"Transformers","text":""},{"location":"api/transformers/#yuyay.transformers","title":"<code>yuyay.transformers</code>","text":"<p>Transformer questions for the YUYAY Intelligence Framework.</p>"},{"location":"api/transformers/#yuyay.transformers.Transformer","title":"<code>Transformer</code>  <code>dataclass</code>","text":"<p>A single transformer question from the YUYAY framework.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>str</code> <p>Short identifier for the question e.g. '1a', '2b', '10'.</p> <code>question</code> <code>str</code> <p>The full text of the transformer question.</p> Source code in <code>yuyay/transformers.py</code> <pre><code>@dataclass\nclass Transformer:\n    \"\"\"A single transformer question from the YUYAY framework.\n\n    Attributes:\n        id: Short identifier for the question e.g. '1a', '2b', '10'.\n        question: The full text of the transformer question.\n    \"\"\"\n\n    id: str\n    question: str\n\n    def summary(self) -&gt; str:\n        \"\"\"Return a formatted summary of this transformer question.\n\n        Returns:\n            A string with the id and question text.\n        \"\"\"\n        return f\"[{self.id}] {self.question}\"\n</code></pre>"},{"location":"api/transformers/#yuyay.transformers.Transformer.summary","title":"<code>summary()</code>","text":"<p>Return a formatted summary of this transformer question.</p> <p>Returns:</p> Type Description <code>str</code> <p>A string with the id and question text.</p> Source code in <code>yuyay/transformers.py</code> <pre><code>def summary(self) -&gt; str:\n    \"\"\"Return a formatted summary of this transformer question.\n\n    Returns:\n        A string with the id and question text.\n    \"\"\"\n    return f\"[{self.id}] {self.question}\"\n</code></pre>"},{"location":"api/transformers/#yuyay.transformers.get_transformer_by_id","title":"<code>get_transformer_by_id(transformer_id)</code>","text":"<p>Look up a transformer question by its id.</p> <p>Parameters:</p> Name Type Description Default <code>transformer_id</code> <code>str</code> <p>The short identifier to search for e.g. '1a', '10'.</p> required <p>Returns:</p> Type Description <code>Transformer | None</code> <p>The matching Transformer, or None if not found.</p> Source code in <code>yuyay/transformers.py</code> <pre><code>def get_transformer_by_id(transformer_id: str) -&gt; Transformer | None:\n    \"\"\"Look up a transformer question by its id.\n\n    Args:\n        transformer_id: The short identifier to search for e.g. '1a', '10'.\n\n    Returns:\n        The matching Transformer, or None if not found.\n    \"\"\"\n    for transformer in ALL_TRANSFORMERS:\n        if transformer.id == transformer_id:\n            return transformer\n    return None\n</code></pre>"},{"location":"api/wheel/","title":"Wheel","text":""},{"location":"api/wheel/#yuyay.wheel","title":"<code>yuyay.wheel</code>","text":"<p>Co-Creation Wheel integration engine for the YUYAY Intelligence Framework.</p>"},{"location":"api/wheel/#yuyay.wheel.WheelResult","title":"<code>WheelResult</code>  <code>dataclass</code>","text":"<p>Result of a Co-Creation Wheel integration.</p> <p>Attributes:</p> Name Type Description <code>sector</code> <code>str</code> <p>The wheel sector being evaluated.</p> <code>worldview_alignment</code> <code>bool</code> <p>Whether the input aligns with the Worldview center.</p> <code>whole_system_score</code> <code>int</code> <p>Score from 0 to 12 based on sectors addressed.</p> <code>notes</code> <code>str</code> <p>Optional contextual notes about the result.</p> Source code in <code>yuyay/wheel.py</code> <pre><code>@dataclass\nclass WheelResult:\n    \"\"\"Result of a Co-Creation Wheel integration.\n\n    Attributes:\n        sector: The wheel sector being evaluated.\n        worldview_alignment: Whether the input aligns with the Worldview center.\n        whole_system_score: Score from 0 to 12 based on sectors addressed.\n        notes: Optional contextual notes about the result.\n    \"\"\"\n\n    sector: str\n    worldview_alignment: bool\n    whole_system_score: int\n    notes: str = \"\"\n\n    def summary(self) -&gt; str:\n        \"\"\"Return a formatted summary of this wheel result.\n\n        Returns:\n            A string describing the sector, alignment, and score.\n        \"\"\"\n        alignment = \"aligned\" if self.worldview_alignment else \"not aligned\"\n        return (\n            f\"[{self.sector}] Worldview: {alignment} | \"\n            f\"Whole System Score: {self.whole_system_score}/12\"\n        )\n</code></pre>"},{"location":"api/wheel/#yuyay.wheel.WheelResult.summary","title":"<code>summary()</code>","text":"<p>Return a formatted summary of this wheel result.</p> <p>Returns:</p> Type Description <code>str</code> <p>A string describing the sector, alignment, and score.</p> Source code in <code>yuyay/wheel.py</code> <pre><code>def summary(self) -&gt; str:\n    \"\"\"Return a formatted summary of this wheel result.\n\n    Returns:\n        A string describing the sector, alignment, and score.\n    \"\"\"\n    alignment = \"aligned\" if self.worldview_alignment else \"not aligned\"\n    return (\n        f\"[{self.sector}] Worldview: {alignment} | \"\n        f\"Whole System Score: {self.whole_system_score}/12\"\n    )\n</code></pre>"},{"location":"api/wheel/#yuyay.wheel.evaluate_whole_system","title":"<code>evaluate_whole_system(sectors_addressed)</code>","text":"<p>Score how many valid wheel sectors are addressed.</p> <p>Parameters:</p> Name Type Description Default <code>sectors_addressed</code> <code>list[str]</code> <p>List of sector names provided by the user.</p> required <p>Returns:</p> Type Description <code>int</code> <p>Integer score from 0 to 12 representing coverage.</p> Source code in <code>yuyay/wheel.py</code> <pre><code>def evaluate_whole_system(sectors_addressed: list[str]) -&gt; int:\n    \"\"\"Score how many valid wheel sectors are addressed.\n\n    Args:\n        sectors_addressed: List of sector names provided by the user.\n\n    Returns:\n        Integer score from 0 to 12 representing coverage.\n    \"\"\"\n    valid = {s.lower() for s in WHEEL_SECTORS}\n    addressed = {s.lower() for s in sectors_addressed}\n    return len(valid &amp; addressed)\n</code></pre>"},{"location":"api/wheel/#yuyay.wheel.get_sector","title":"<code>get_sector(sector_name)</code>","text":"<p>Look up a wheel sector by name.</p> <p>Parameters:</p> Name Type Description Default <code>sector_name</code> <code>str</code> <p>The name of the sector to search for.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>The matching sector name, or None if not found.</p> Source code in <code>yuyay/wheel.py</code> <pre><code>def get_sector(sector_name: str) -&gt; str | None:\n    \"\"\"Look up a wheel sector by name.\n\n    Args:\n        sector_name: The name of the sector to search for.\n\n    Returns:\n        The matching sector name, or None if not found.\n    \"\"\"\n    for sector in WHEEL_SECTORS:\n        if sector.lower() == sector_name.lower():\n            return sector\n    return None\n</code></pre>"}]}